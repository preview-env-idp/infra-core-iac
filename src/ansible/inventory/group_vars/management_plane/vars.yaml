---
# Don't change unless you know what you're doing:
alcambic_devbox_m2m_key_path: "{{ '~' | expanduser }}/.ssh/id_ed25519_alcambic_devbox_m2m"
ansible_ssh_private_key_file: "{{ alcambic_devbox_m2m_key_path }}"
ansible_become_password: "{{ vault_devbox_alcambic_password }}"
