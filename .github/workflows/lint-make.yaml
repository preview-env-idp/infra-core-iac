---
name: Makefile Lint

on:
  pull_request:

permissions:
  contents: read

jobs:
  lint:
    name: Validate Makefile
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: 'stable'

      - name: Warn about space indentation in Makefile
        run: |
          awk '/^ +(@|[a-zA-Z0-9-])/ { print "::warning file=Makefile,line=" NR "::[Line " NR "] Potential indentation issue (spaces instead of tabs): " $0 }' Makefile

      - name: Install checkmake
        run: go install github.com/checkmake/checkmake/cmd/checkmake@latest

      - name: Run checkmake
        run: checkmake --config=.checkmake Makefile
